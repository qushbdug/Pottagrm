# 🤝 دليل المساهمة - Yemen Net Bot

شكراً لاهتمامك بالمساهمة في مشروع بوت يمن نت! هذا الدليل سيساعدك على البدء.

## 📋 قبل البدء

- تأكد من قراءة [README.md](README.md) أولاً
- تحقق من [CHANGELOG.md](CHANGELOG.md) لمعرفة آخر التحديثات
- اقرأ هذا الدليل بالكامل

## 🚀 كيفية المساهمة

### 1. الإبلاغ عن الأخطاء

إذا وجدت خطأ:

1. تحقق من [Issues](https://github.com/your-username/yemen-net-bot/issues) أولاً
2. إذا لم يكن موجوداً، أنشئ Issue جديد
3. استخدم قالب Bug Report
4. ارفق سجلات الخطأ والخطوات لإعادة الإنتاج

### 2. اقتراح ميزات جديدة

لاقتراح ميزة جديدة:

1. تحقق من [Issues](https://github.com/your-username/yemen-net-bot/issues) أولاً
2. أنشئ Issue جديد باستخدام قالب Feature Request
3. وصف الميزة بالتفصيل
4. اشرح لماذا هذه الميزة مفيدة

### 3. المساهمة بالكود

#### إعداد بيئة التطوير

```bash
# استنساخ المستودع
git clone https://github.com/your-username/yemen-net-bot.git
cd yemen-net-bot

# إعداد البيئة
make dev-setup

# تشغيل الاختبارات
make test
```

#### إرشادات الكود

- استخدم **Python 3.8+**
- اتبع **PEP 8** لتصميم الكود
- اكتب **تعليقات بالعربية** للوظائف المهمة
- استخدم **type hints** عند الإمكان
- اكتب **docstrings** للدوال

#### هيكل المشروع

```
bot/
├── config.py          # إعدادات البوت
├── database/          # قاعدة البيانات
├── models/            # نماذج البيانات
├── services/          # الخدمات
└── utils/             # أدوات مساعدة
```

#### إضافة ميزة جديدة

1. **إنشاء نموذج** في `bot/models/`
2. **إضافة الخدمة** في `bot/services/`
3. **تحديث البوت** في `main_bot.py`
4. **إضافة الاختبارات** في `tests/`
5. **تحديث الوثائق**

### 4. إرسال Pull Request

#### قبل إرسال PR

- ✅ تشغيل الاختبارات: `make test`
- ✅ تشغيل التنسيق: `make format`
- ✅ تشغيل الفحص: `make lint`
- ✅ تحديث الوثائق
- ✅ إضافة اختبارات للميزات الجديدة

#### إرشادات PR

1. **عنوان واضح** يصف التغيير
2. **وصف مفصل** للتغييرات
3. **ربط Issues** ذات الصلة
4. **لقطات شاشة** إذا كان مناسباً
5. **اختبار التغييرات** محلياً

## 🧪 الاختبارات

### تشغيل الاختبارات

```bash
# جميع الاختبارات
make test

# اختبار محدد
pytest tests/test_bot.py -v

# اختبار مع تغطية
pytest --cov=bot tests/
```

### كتابة اختبارات جديدة

- استخدم **pytest** كإطار اختبار
- اكتب اختبارات **واضحة ومفهومة**
- غطي **الحالات الحدية**
- استخدم **mocks** للخدمات الخارجية

## 📝 الوثائق

### تحديث الوثائق

- **README.md** - تحديث عند إضافة ميزات جديدة
- **CHANGELOG.md** - إضافة التغييرات
- **docstrings** - تحديث تعليقات الكود
- **تعليقات** - شرح المنطق المعقد

### إرشادات الكتابة

- استخدم **العربية** للوثائق
- اكتب **أمثلة عملية**
- استخدم **رموز تعبيرية** للوضوح
- اربط **الملفات ذات الصلة**

## 🔧 الأدوات

### Makefile

```bash
make help          # عرض جميع الأوامر
make install       # تثبيت المتطلبات
make run           # تشغيل البوت
make test          # تشغيل الاختبارات
make format        # تنسيق الكود
make lint          # فحص الكود
make clean         # تنظيف الملفات المؤقتة
```

### Pre-commit Hooks

```bash
# تثبيت pre-commit
pip install pre-commit

# إعداد hooks
pre-commit install

# تشغيل على جميع الملفات
pre-commit run --all-files
```

## 📞 التواصل

### قنوات التواصل

- **GitHub Issues** - للأخطاء والميزات
- **GitHub Discussions** - للمناقشات العامة
- **Telegram** - @YemenNetSupport
- **Email** - support@yemen-net.com

### اجتماعات الفريق

- **اجتماع أسبوعي** كل يوم أحد
- **مراجعة الكود** عند إرسال PR
- **تحديثات منتظمة** عبر GitHub

## 🎯 أولويات التطوير

### عالية الأولوية
- 🐛 إصلاح الأخطاء الحرجة
- 🔒 تحسين الأمان
- 📱 تحسين واجهة المستخدم

### متوسطة الأولوية
- 🆕 ميزات جديدة مفيدة
- 📊 تحسين الأداء
- 🧪 زيادة تغطية الاختبارات

### منخفضة الأولوية
- 🎨 تحسينات التصميم
- 📝 تحسين الوثائق
- 🔧 تحسينات الأدوات

## 📋 قائمة التحقق

قبل إرسال مساهمة:

- [ ] قرأت README.md
- [ ] قرأت هذا الدليل
- [ ] اختبرت التغييرات محلياً
- [ ] تشغيل الاختبارات
- [ ] تشغيل التنسيق
- [ ] تشغيل الفحص
- [ ] حدثت الوثائق
- [ ] كتبت رسالة commit واضحة
- [ ] ربطت Issues ذات الصلة

## 🙏 شكراً لك!

شكراً لمساهمتك في جعل بوت يمن نت أفضل! كل مساهمة، مهما كانت صغيرة، مهمة جداً لنا.

---

**فريق يمن نت** 🚀